# Quintessence Gallery: Plan & Living Checklist

> **Purpose:** Add a photo gallery to the React Quintessence page (`/quintessence` in `nashirj/`),
> using the photos in `quintessence/images/`. Follows the same conventions as `PORTING_PLAN.md`
> (which is fully DONE — the ported site is live at www.nashirj.com).
>
> **HOW TO USE THIS FILE:** Work phases in order. Set status `TODO` → `IN PROGRESS` → `DONE`,
> check off tasks as you go, run each phase's Acceptance check, and fill in Handoff notes.

---

## Context & source material

- **Legacy gallery:** `quintessence/html/gallery.htm` — a Lightbox2 (Lokesh Dhakar) page with
  **two captioned sections**: *"Onsite Testing at Johnson Space Center"* (~20 photos) and
  *"Prototype Development"* (~15 photos). Full image→caption manifest is in **Appendix A** below —
  don't re-parse the HTML.
- **Current React page:** `nashirj/src/containers/quintessence/QuintessenceContent.js` (+ `.css`),
  data in the `quintessence` export of `nashirj/src/portfolio.js`. Single centered column,
  max-width 800px, uses `theme` prop for colors.
- **Image source dir:** `quintessence/images/` — **131 MB, must NOT be bulk-copied** (locked
  decision from PORTING_PLAN). Many originals are huge (e.g. `NBL.jpg` is 5.7 MB at 5568×3712,
  `team-1.png` is 15 MB). Only 3 images are currently in `nashirj/public/quintessence-images/`
  (`header.jpg`, `team-5.jpg`, `team-before-test.jpg`).

## Locked decisions (do not relitigate)

1. **The gallery is a section on the existing `/quintessence` page**, not a new route. The
   PORTING_PLAN locked "single standalone summary page" for Quintessence; this stays true —
   the gallery renders below the existing overview content, above the "Back to Technical" link.
2. **Curated set = the legacy gallery's image list** (Appendix A, 36 images). `quintessence/images/`
   contains extra photos the old gallery never showed (`team-1.png`, `diver-debrief-4.JPG`,
   `clamp-handrail-2.jpg`, `final-clamp.JPG`, individual member headshots, logo drafts, etc.) —
   do **not** add them without owner sign-off; the member headshots especially were intentionally
   excluded elsewhere in the port (see PORTING_PLAN Phase 5 notes on member photos).
3. **No new npm dependencies.** The app is CRA `react-scripts 3.2.0` on a modern React — old
   lightbox packages (react-image-lightbox, lightbox2 wrappers) are unmaintained and risky here.
   Build a small custom `<Lightbox>` component instead (it's ~100 lines; spec in Phase 2).
4. **Resized derivatives only in `public/` — never the originals.** Two sizes per image
   (thumbnail + lightbox), generated by script into `nashirj/public/quintessence-images/gallery/`.
   Total budget ≤ ~15 MB. Originals stay untouched in `quintessence/images/` (still the reference,
   still not to be deleted until the final legacy cleanup).
5. **Data model follows the port convention:** the image manifest lives in `portfolio.js` as a
   `quintessenceGallery` export (array of `{ id, thumb, full, caption, section }`); the gallery
   component is thin and maps over it.

---

## Progress tracker

- [ ] **Phase 1** — Generate optimized gallery images · status: **TODO**
- [ ] **Phase 2** — Gallery grid + lightbox components · status: **TODO**
- [ ] **Phase 3** — Wire into Quintessence page, polish, verify, deploy · status: **TODO**

---

## Phase 1 — Generate optimized gallery images
**Prereqs:** none · **Status:** TODO

Goal: every Appendix A image exists in `nashirj/public/quintessence-images/gallery/` in two sizes,
with normalized names, at a total size that won't bloat the repo or the page.

- [ ] Write a one-off script (bash + `sips`, which is built into macOS, or ImageMagick if
      available) in the scratchpad — it does **not** get committed. For each Appendix A image:
      - **Thumbnail** → `gallery/thumbs/<name>.jpg`: max dimension **480px**, JPEG quality ~75.
      - **Lightbox size** → `gallery/full/<name>.jpg`: max dimension **1600px**, JPEG quality ~80.
      - **Normalize names**: lowercase kebab-case, `.jpg` extension for everything (the source
        mixes `.jpg`/`.JPG`/`.png`; `finite-element-analysis.png` becomes a jpg too — it's a
        photo-like render, jpg is fine). E.g. `team-mission-control.JPG` → `team-mission-control.jpg`.
      - Strip EXIF metadata if the tool supports it (`sips` drops most of it on resize by default).
      - **Respect EXIF orientation** — verify a few portrait shots aren't rotated sideways after
        resize (sips honors orientation; ImageMagick needs `-auto-orient`).
- [ ] Sanity-check output: `du -sh` each dir (thumbs should be well under 2 MB total; full set
      under ~13 MB), spot-open ~5 images including at least one `.JPG`-source and the `.png`-source.
- [ ] Record final sizes + the exact resize commands in Handoff notes (so images can be
      regenerated or new ones added consistently later).

**Acceptance check:** 36 thumbs + 36 full-size files exist under
`nashirj/public/quintessence-images/gallery/{thumbs,full}/`, all lowercase `.jpg`, combined size
≤ 15 MB, no sideways-rotated images.

**Handoff notes:** _(fill in)_

---

## Phase 2 — Gallery grid + lightbox components
**Prereqs:** Phase 1 DONE · **Status:** TODO

Goal: reusable, dependency-free gallery UI matching the site's theme.

- [ ] **`portfolio.js`:** add `quintessenceGallery` export — array of
      `{ id, thumb, full, caption, section }` built from Appendix A. Paths root-relative
      (`/quintessence-images/gallery/thumbs/….jpg`), per the port's asset convention. Two section
      values: `"jsc"` and `"prototype"`, with display titles kept in the component
      (`"Onsite Testing at Johnson Space Center"`, `"Prototype Development"`).
      **Caption fix to apply while transcribing:** the legacy caption for
      `finite-element-analysis.png` ("The CLaMP on the test stand outside of the Fullerton College
      pool") is clearly a copy-paste error — the file is an FEA render. Use
      "Finite element analysis of the CLaMP" and note it in Handoff notes for owner review.
- [ ] **`GalleryGrid` component** (`src/components/galleryGrid/GalleryGrid.js` + `.css`):
      - Responsive CSS grid of thumbnails: `repeat(auto-fill, minmax(160px, 1fr))`, square-ish
        cells (`aspect-ratio` + `object-fit: cover`), small gap; collapses to 2 columns on mobile.
      - Every `<img>` gets `loading="lazy"` and `alt={caption}` — 36 images must not all load
        eagerly.
      - Thumbnails are `<button>`s (not bare `<img>`/`<a>`) so they're keyboard-focusable;
        clicking opens the lightbox at that index.
      - Renders one grid per section with an `<h3>` section title styled with `theme.text`,
        mirroring the legacy page's two-section structure.
- [ ] **`Lightbox` component** (`src/components/lightbox/Lightbox.js` + `.css`), custom, no deps:
      - Fixed full-viewport overlay (dark scrim), centered `full`-size image
        (`max-width/max-height` ~90vw/85vh, `object-fit: contain`), caption below, ×-close button,
        ‹ › prev/next buttons, "n / 36" counter.
      - Keyboard: `Escape` closes, `ArrowLeft`/`ArrowRight` navigate (add/remove a `keydown`
        listener in an effect). Clicking the scrim (outside the image) closes.
      - Prev/next wraps around and moves through the **combined** list across both sections,
        like the legacy `data-lightbox="example-set"` behavior.
      - Lock body scroll while open (`document.body.style.overflow = "hidden"`, restored on
        close/unmount).
      - Basic a11y: `role="dialog"` + `aria-modal="true"`, `aria-label` from the caption, move
        focus into the dialog on open and back to the invoking thumbnail on close.
      - State (`openIndex` or `null`) lives in the parent (`GalleryGrid` or the container);
        `Lightbox` is controlled via props (`items`, `index`, `onClose`, `onPrev`, `onNext`).
- [ ] Match existing style conventions: class-name prefixes (`gallery-`, `lightbox-`), theme
      colors via the `theme` prop (scrim/controls can be hardcoded dark — a lightbox scrim is
      dark in both themes), CSS files co-located like every other component.

**Acceptance check:** Components compile with zero new warnings; grid renders both sections from
`quintessenceGallery`; lightbox opens/closes/navigates via mouse **and** keyboard; body doesn't
scroll behind it; no console errors.

**Handoff notes:** _(fill in)_

---

## Phase 3 — Wire into the Quintessence page, verify, deploy
**Prereqs:** Phase 2 DONE · **Status:** TODO

- [ ] Render the gallery in `QuintessenceContent.js`: a "Gallery" `<h2>` + `<GalleryGrid …/>`
      between the existing body text (after the contact line) and the "← Back to Technical" link.
      Pass `theme` through like the rest of the page. Widen only the gallery block if the 800px
      column feels cramped (e.g. gallery wrapper `max-width: 1000px`) — don't restyle the
      existing prose column.
- [ ] Responsive pass at 375px and 1280px: no horizontal overflow, thumbs readable on mobile,
      lightbox controls reachable/tappable on mobile.
- [ ] `npm run build` clean (zero warnings — the build is currently warning-free; keep it that way).
- [ ] Serve the production build (`serve -s build`), verify `/quintessence` end-to-end: thumbs
      load lazily (check Network tab), a few full-size images open in the lightbox, keyboard nav
      works, spot-check 200s on e.g. `/quintessence-images/gallery/thumbs/nbl.jpg` and
      `/quintessence-images/gallery/full/nbl.jpg`.
- [ ] Commit; then **ask the owner before deploying** (`npm run deploy` from `nashirj/` publishes
      to the live site).
- [ ] After deploy (if approved): smoke-test `https://www.nashirj.com/quintessence` — gallery
      renders, one thumb and one full image return 200, lightbox works.

**Acceptance check:** Live (or locally-served production) `/quintessence` shows the two-section
gallery; lightbox fully functional with mouse + keyboard; build clean; no console errors; page
weight on initial load not blown up (lazy thumbs, full images fetched only on lightbox open).

**Handoff notes:** _(fill in)_

---

# Appendix A — Gallery manifest (from `quintessence/html/gallery.htm`)

> Order below = legacy display order. Captions are the legacy `data-title` values, verbatim except
> where noted. Source files live in `quintessence/images/`; note the mixed-case extensions.

## Section 1 — "Onsite Testing at Johnson Space Center" (21 images)

| # | Source file | Caption |
|---|---|---|
| 1 | `clayton-anderson-1.jpg` | Clayton Anderson, former astronaut |
| 2 | `clayton-anderson-2.jpg` | Clayton Anderson, former astronaut |
| 3 | `clayton-anderson-3.JPG` | Clayton Anderson, former astronaut |
| 4 | `diver-debrief-1.jpg` | Debriefing divers on proper usage of tool |
| 5 | `diver-debrief-2.jpg` | Debriefing divers on proper usage of tool |
| 6 | `diver-debrief-3.jpg` | Debriefing divers on proper usage of tool |
| 7 | `divers-post-testing.jpg` | Divers discussing impressions of tool usability and performance |
| 8 | `divers.jpg` | Divers in the NBL prior to testing |
| 9 | `jsc-everyone.jpg` | Micro-G NExT test week 1 participants in front of NBL |
| 10 | `team-2.JPG` | Quintessence |
| 11 | `team-3.JPG` | Quintessence |
| 12 | `team-4.jpg` | Quintessence |
| 13 | `team-5.jpg` | Quintessence |
| 14 | `team-w-friends.jpg` | Quintessence with coop interns at Johnson Space Center |
| 15 | `team-w-spacesuit.jpg` | Quintessence with a used spacesuit at the NBL spacesuit lab |
| 16 | `team-mission-control.JPG` | Quintessence in JSC's mission control, watching ISS operations in real time |
| 17 | `team-w-divers.JPG` | Quintessence with NBL divers after testing |
| 18 | `testing-1.jpg` | Quintessence in NBL control room during testing |
| 19 | `testing-2.jpg` | Quintessence in NBL control room during testing |
| 20 | `testing-3.jpg` | Quintessence in NBL control room during testing |
| 21 | `NBL.jpg` | The NBL, where the CLaMP was tested |

## Section 2 — "Prototype Development" (15 images)

| # | Source file | Caption |
|---|---|---|
| 1 | `clamp-ceta.jpg` | The CLaMP being tested by team member Alex Miyoshi in the Fullerton College pool |
| 2 | `clamp-handrail-1.jpg` | The CLaMP being tested by team member Alex Miyoshi in the Fullerton College pool |
| 3 | `finite-element-analysis.png` | ⚠️ legacy caption is a copy-paste error ("test stand outside of the Fullerton College pool") — use "Finite element analysis of the CLaMP" (flag for owner) |
| 4 | `yaw-v3.jpg` | Final version of yaw mechanism |
| 5 | `yaw-v2.jpg` | Second version of yaw mechanism |
| 6 | `yaw-v1.jpg` | First version of yaw mechanism |
| 7 | `arm-v4.jpg` | Final version of arm mechanism |
| 8 | `arm-v3.jpg` | Third version of arm mechanism |
| 9 | `arm-v2.jpg` | Second version of arm mechanism |
| 10 | `arm-v1.jpg` | First version of arm mechanism |
| 11 | `screw-knob-new.jpg` | New version of screw knob |
| 12 | `screw-knob-old.jpg` | Old version of screw knob |
| 13 | `clamp-knob-new.jpg` | New version of clamp knob |
| 14 | `clamp-knob-old.jpg` | Old version of clamp knob |
| 15 | `final-clamp-poc.jpg` | Final proof of concept rendition of CLaMP |

**Total: 36 images** (21 + 15).

## Not in the legacy gallery (do NOT add without owner sign-off)

`team-1.png` (15 MB), `diver-debrief-4.JPG`, `clamp-handrail-2.jpg`, `clamp.jpg` (dupe of
`clamp-handrail-1.jpg` by size), `final-clamp.JPG`, `final-clamp-poc` vs `final-clamp` variants,
`team.jpg`, `team-with-friends.jpg` (near-dupe naming), `micro-g-finalists.jpg`,
member headshots (`alex.jpg`, `george.jpg`, `guthrie.jpg`, `ian.jpg`, `jonathan.jpg`,
`michelle.jpg`, `nashir.jpg`, `patrick.jpg`, `saleh.jpg`), logo drafts, `astro-ai.png`,
`unbounded.jpeg`, images already used elsewhere on the page (`header.jpg`,
`team-before-test.jpg` — don't repeat them in the grid), and the old
Lightbox UI sprites (`close.png`, `next.png`, `prev.png`, `loading.gif`).
